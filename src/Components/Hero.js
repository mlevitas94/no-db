import React from 'react'

export default function Hero(props){
        return(
            <div className="HeroBox">
                <h2>{this.props.name} </h2>
                {/*img of the hero*/}
                <p>{this.props.role}</p>
            </div>
        )
}